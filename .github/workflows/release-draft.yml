name: Draft Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: BePartnerLabs/centralized-workflows/.github/workflows/release-drafter.yml@v1
